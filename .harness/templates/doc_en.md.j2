# {{ title_en }}

## 📌 Overview
{{ description_en }}

```mermaid
sequenceDiagram
    autonumber
    actor Sender as Sender (Alice)
    actor Receiver as Receiver (Bob)

    Note over Sender: 1. Generate ephemeral symmetric key & parameters
    Note over Sender: 2. Encapsulate / wrap key with Receiver Public Key
    Sender->>Receiver: Transmit encapsulated key & encrypted payload
    Note over Receiver: 3. Decapsulate / unwrap key with Receiver Private Key & decrypt
```

---

## 🔍 Core Engineering Mechanisms
Clear, practical explanation of data flow and operational mechanisms.

---

## 💻 Runnable Multi-Language Implementation Tabs

=== "Python"

    ```python
    # Python 3 + cryptography library
    print("Running {{ slug }} in Python...")
    ```

=== "Rust"

    ```rust
    // Rust 2021/2024 Edition
    fn main() {
        println!("Running {{ slug }} in Rust...");
    }
    ```

=== "C++"

    ```cpp
    // C++20 + OpenSSL 3.5 EVP API
    #include <iostream>

    int main() {
        std::cout << "Running {{ slug }} in C++20...\n";
        return 0;
    }
    ```

=== "OpenSSL CLI"

    ```bash
    # OpenSSL 3.5 CLI
    openssl version
    ```
