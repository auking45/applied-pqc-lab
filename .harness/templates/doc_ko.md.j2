# {{ title_ko }}

## 📌 개요
{{ description_ko }}

```mermaid
sequenceDiagram
    autonumber
    actor Sender as 송신자 (Alice)
    actor Receiver as 수신자 (Bob)

    Note over Sender: 1. 일회용 대칭키(DEK) 및 보안 파라미터 생성
    Note over Sender: 2. 수신자 공개키로 키 캡슐화/래핑
    Sender->>Receiver: 암호화된 키 및 페이로드 전송
    Note over Receiver: 3. 수신자 개인키로 역캡슐화 및 복호화
```

---

## 🔍 핵심 엔지니어링 메커니즘
이해하기 쉬운 비유와 실무 아키텍처 관점에서 데이터 흐름과 동작 원리를 서술한다.

---

## 💻 실행 가능한 4개 언어 검증 코드

=== "Python"

    ```python
    # Python 3 + cryptography library
    print("Running {{ slug }} in Python...")
    ```

=== "Rust"

    ```rust
    // Rust 2021/2024 Edition
    fn main() {
        println!("Running {{ slug }} in Rust...");
    }
    ```

=== "C++"

    ```cpp
    // C++20 + OpenSSL 3.5 EVP API
    #include <iostream>

    int main() {
        std::cout << "Running {{ slug }} in C++20...\n";
        return 0;
    }
    ```

=== "OpenSSL CLI"

    ```bash
    # OpenSSL 3.5 CLI
    openssl version
    ```
